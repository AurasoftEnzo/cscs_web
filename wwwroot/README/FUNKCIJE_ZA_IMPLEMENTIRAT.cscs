// FUNKCIJE ZA IMPLEMENTIRAT

hndlVar = LoadHtmlFile("..\\path\\file.html");

SendDefinesToHtml(hndlVar);

ConditionalRender(hndlVar, "listaId", showButtonOrImage % 2 == 0);

FillHtmlUsingDictionary(hndlVar, valuesDictionary);

FillPlaceholder(hndlVar, "placeholderName", "new value for placeholder");

finalHtmlString = RenderHtml(hndlVar);

//------------------

function nesto(args){
    value1 = args["hashKey"] // IMPLEMENTIRAT
}